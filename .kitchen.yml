---
driver:
  name: vagrant

provisioner:
  name: chef_solo

verifier:
  name: inspec

platforms:
  - name: debian-9.2

suites:
  - name: default
    run_list:
      - recipe[codenamephp_php::default]
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
  - name: "7.1"
    run_list:
      - recipe[codenamephp_php::7.1]
    verifier:
      inspec_tests:
        - test/smoke/7.1
    attributes: